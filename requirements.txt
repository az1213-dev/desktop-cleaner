# ==============================================================================
# ClutterCtrl - Terminal Dependencies
# ==============================================================================
# Core CLI and per-run logging require ZERO third-party packages (Python stdlib only).
#
# Optional extras:
# For background folder watcher:  pip install watchdog
# For dev & testing:              pip install pytest
#
# Or install in editable mode:
# pip install -e .
# ==============================================================================

# --- Optional Background Watcher ---
watchdog>=4.0.0
